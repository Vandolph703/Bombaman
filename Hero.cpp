#define _USE_MATH_DEFINES
#include <math.h>
#include "Hero.h"

/*
void Hero::draw(){
    //glColor3f(r, g, b);
    
    glBegin(GL_POLYGON);
    
	glVertex2f(x, y);
	glVertex2f(x + width, y);
	glVertex2f(x + width, y - height);
	glVertex2f(x, y - height);
    
    glEnd();
}

void dead(){			//If player dead, game over	//void or bool?
	//player is dead
}
void placeBomb() {		//basics
	//put bomb and start timer 
}
void kickBomb() {		//Power up, moves bomb
	/*
	if contains = true
		move bomb in certain direction
	
	*/
}
*/